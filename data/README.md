# Encrypted Publish Artifacts

This folder is intended for generated encrypted files only.

Expected generated outputs:

- `data/manifest.enc.json`
- `data/media/*.enc.json`

Generate them with:

```bash
python3 tools/build_profile.py
```
